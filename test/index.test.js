describe('jest单测启动', () => {
  test('jest单测启动', () => {
    // const wrapper = shallow(<Index data={{ title: '标题' }} />);
    // expect(wrapper.find('.title').length).toEqual(1);
    expect(1).toEqual(1);
  });
});
